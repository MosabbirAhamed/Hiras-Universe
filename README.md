# Hira's Universe — Frontend Scaffold

This repository contains an initial Next.js + TypeScript + Tailwind scaffold for Hira's Universe. Mobile-first design is enforced in components and layout patterns.

Run locally:

```bash
npm install
npm run dev
```
